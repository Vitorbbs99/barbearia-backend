# Backend - API para gestão de BARBEARIA

API construída com Node.js + Express.

## Como rodar localmente

1. Instale as dependências:

   ```bash
   npm install

2. Certifique-se de ter um banco MySQL criado antes de iniciar o servidor.

3. Para rodar:

   ```bash
   node server.js